/*
  * You may assume the node class is:
  * class LLNode {
  *   constructor(value, next = null) {
  *     this.value = value;
  *     this.next = next;
  *   }
  * }
  * 
  * check if next node is null, 
  * if next node isn't null, current val = next, next val = next next
  * if next next is null, delete current val
  */


class LLNode (

    function deleteMiddleNode(head) {
        if(head.next = )
      }
)
